# Requires Python 3.10+  (uses union type syntax: X | Y, list[X])
# System requirement: ffmpeg must be installed (for audio mixing with intro/outro)
#   macOS:  brew install ffmpeg
#   Ubuntu: apt-get install ffmpeg
#   Windows: winget install ffmpeg

fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
pydantic==2.10.3
pydantic-settings==2.6.1
elevenlabs==1.9.0
pydub==0.25.1
audioop-lts>=0.2.1  # Python 3.13 backport of audioop (required by pydub)
python-dotenv==1.0.1
httpx==0.28.0
aiofiles==24.1.0
loguru==0.7.3
anthropic>=0.49.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
